/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.icons8.com','res.cloudinary.com'], // Add other domains if needed
    },
}

module.exports = nextConfig
